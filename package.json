{
  "name": "grab-lyrics",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "nyc mocha --recursive",
    "coverage": "nyc report --reporter=lcov > coverage.lcov && codecov"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^1.0.0-rc.2",
    "codecov": "^3.0.0",
    "node-fetch": "^1.7.3"
  },
  "devDependencies": {
    "mocha": "^5.0.0",
    "nyc": "^11.4.1"
  }
}
